$('.required').removeClass('required');
if ( $( '#fields_537' ).length > 0 ) $( '#fields_537' ).val( 'INCOMPLETE RECORD - Used for form field testing - Required fields were overridden' );
$( 'form' ).submit();
